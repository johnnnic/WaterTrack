FROM node:20-alpine

WORKDIR /app

EXPOSE 8080

# node_modules dikelola oleh named volume (tidak tercampur dengan host)
# npm install dijalankan saat container start agar selalu sinkron dengan package.json
CMD ["sh", "-c", "npm install && npm run dev"]
